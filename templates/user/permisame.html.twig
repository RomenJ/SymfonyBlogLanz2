{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cambiar permisos del usuario {{ user.username }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}