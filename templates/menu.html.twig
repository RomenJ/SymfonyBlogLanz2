  {% if app.user %}
   <div class="MenuIni">
 {# <img class="foto" width="100x" src="/images/sf.png"> #}
         <h6 class="Negro"> Usuario registrado : {{ app.user.username }} </h6>
         <img class="fotoAvatar" width="100x" src="/images/{{ app.user.avatar }} "> 
        <h6 class="Logouth"><a href="{{ path('app_logout') }}">Logout</a> </h6>         
         {# 
         <p class="Negro"> >Tipo de Usuario : {{ app.user.roles }} </p>  #}
         
       {#       <li>Mida el dolor de la sesión <code><a href="{{ path('app_evamax_index') }}"> Realizar Evamax</a></code></li>      #}
        
        {% if is_granted('ROLE_ADMIN') %}
                <a class="btn" href="{{ path('app_foro_index') }}"> Panel de foro</a>
                <a class="btn" href="{{ path('app_user_index') }}"> Panel de users </a>
                <a class="btn" href="{{ path('app_user_banearlist') }}"> Bannerar Users</a>
                <a class="btn" href="{{ path('app_user_permissonlist') }}"> Cambiar permisos</a>
                <a class="btn2" href="{{ path('app_tema_moderafulllist') }}"> Moderar temas</a>
                <a class="btn2" href="{{ path('app_tema_admintemafullist') }}"> Administrar temas</a>
                <a class="btn3" href="{{ path('app_comentario_listmodera') }}"> Administrar comentarios</a>
                <a class="btn4" href="{{ path('app_comentario_index') }}"> Ver comentarios</a>
         {% endif %} 
        
{#
          <a  class="btn"  href="{{ path('app_tema_index') }}"> Panel de temas</a>
          <a  class="btn"  href="{{ path('app_comentario_index') }}"> Panel de comentarios</a> #}

      
     
   </div>
   

 {% else %}
    <div class="MenuIni">
      <ul>
      <p class="Negro"> Usuario no registrado</p>
        <a class="btn" href="{{ path('app_register') }}"> Registro</a>
        <a  class="btn" href="{{ path('app_login') }}"> Login</a>
        <a class="btn4" href="{{ path('app_comentario_index') }}"> Ver comentarios</a>
    </ul>
 </div>
     {% endif %}